# Salesforce metadata

The `force-app` directory will hold approved Salesforce source metadata retrieved from and deployed through controlled environments.

No metadata has been retrieved yet. Before retrieval, approve the package scope, source-tracking approach, sensitive-file exclusions, and baseline worklog.
